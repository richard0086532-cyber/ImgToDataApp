flask==3.0.3
flask-cors==4.0.1
Pillow==10.3.0
numpy==1.26.4
gunicorn==23.0.0

# 真实图表提取依赖（注意：PaddleOCR模型很大，Render免费版512MB内存可能不足）
rapidocr-onnxruntime==1.3.24
opencv-python==4.11.0.86
pandas==2.2.3
scipy==1.15.2

# 如果Render免费版内存不足，可尝试用轻量版替代（精度可能降低）：
# rapidocr-onnxruntime==1.3.24
